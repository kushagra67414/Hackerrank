#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {
    char str[1000];
    int n ,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%s",str);
    
    for (int i=0;i=strlen(str);i=i+2)
    printf("%c",str[]);
    printf("\t");
    for (int i=1;i=strlen(str);i=i+2)
    printf("%c",str[]);
    return 0;
}
